---
phase: 04-multi-language-support
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - source/MawaqitWidgetView.mc
autonomous: false
requirements:
  - LOC-03

must_haves:
  truths:
    - "French device sees Widget countdown header with 'dans' instead of 'in'"
    - "French device sees Widget empty state no-mosque message in French"
    - "French device sees Widget no-data placeholder in French"
    - "Widget prayer names remain Fajr, Dhuhr, Asr, Maghrib, Isha"
  artifacts:
    - path: "source/MawaqitWidgetView.mc"
      provides: "Widget view using loadResource for all display strings"
      contains: "Rez.Strings.CountdownIn"
  key_links:
    - from: "source/MawaqitWidgetView.mc"
      to: "resources/strings/strings.xml"
      via: "WatchUi.loadResource(Rez.Strings.*)"
      pattern: "loadResource.*Rez\\.Strings"
    - from: "source/MawaqitWidgetView.mc"
      to: "source/PrayerLogic.mc"
      via: "formatCountdown(remaining, name, tokenIn, tokenNow)"
      pattern: "formatCountdown.*tokenIn.*tokenNow"
---

<objective>
Localize the Widget view surface and verify the complete localization visually.

Purpose: Completes the localization of all three app surfaces (Glance done in Plan 01, Settings done automatically via string resources in Plan 01, Widget done here). The checkpoint verifies the entire localization works end-to-end.

Output: Fully localized MawaqitWidgetView.mc using loadResource() for all display strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-language-support/04-CONTEXT.md
@.planning/phases/04-multi-language-support/04-RESEARCH.md
@.planning/phases/04-multi-language-support/04-01-SUMMARY.md

@source/MawaqitWidgetView.mc
@source/PrayerLogic.mc
@resources/strings/strings.xml
@resources-fre/strings/strings.xml

<interfaces>
<!-- From Plan 01: PrayerLogic.formatCountdown new signature -->
```monkey-c
// source/PrayerLogic.mc (modified in Plan 01)
function formatCountdown(remainingSec as Number, prayerName as String,
                          tokenIn as String, tokenNow as String) as String
```

<!-- From Plan 01: String resource IDs available -->
```
Rez.Strings.CountdownIn     -- "in" (EN) / "dans" (FR)
Rez.Strings.CountdownNow    -- "now" (EN) / "maintenant" (FR)
Rez.Strings.WidgetNoMosqueLine1  -- "Set mosque in" (EN) / "Configurer la mosquee" (FR)
Rez.Strings.WidgetNoMosqueLine2  -- "Garmin Connect app" (EN) / "dans Garmin Connect" (FR)
Rez.Strings.NoDataPlaceholder    -- "-- in --" (EN) / "-- dans --" (FR)
```

<!-- From MawaqitWidgetView.mc: Current hardcoded strings to replace -->
Lines 66-73 (no-mosque empty state):
```monkey-c
"Set mosque in",
"Garmin Connect app",
```

Lines 107-113 (countdown header calls):
```monkey-c
countdownText = PrayerLogic.formatCountdown(0, result["name"] as String);
countdownText = PrayerLogic.formatCountdown(result["remaining"] as Number, result["name"] as String);
```

Line 246 (no-data empty state):
```monkey-c
"-- in --",
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Localize MawaqitWidgetView with loadResource()</name>
  <files>source/MawaqitWidgetView.mc</files>
  <read_first>source/MawaqitWidgetView.mc, source/PrayerLogic.mc, resources/strings/strings.xml, resources-fre/strings/strings.xml, .planning/phases/04-multi-language-support/04-CONTEXT.md</read_first>
  <action>
**1. No-mosque empty state (lines 55-76 in `onUpdate()`).**

Replace the hardcoded `"Set mosque in"` (line 66) and `"Garmin Connect app"` (line 73) with loadResource() calls:

```monkey-c
var noMosqueLine1 = WatchUi.loadResource(Rez.Strings.WidgetNoMosqueLine1) as String;
var noMosqueLine2 = WatchUi.loadResource(Rez.Strings.WidgetNoMosqueLine2) as String;
```

Then use `noMosqueLine1` and `noMosqueLine2` in the respective `dc.drawText()` calls.

The `"Mawaqit"` on line 60 stays hardcoded -- it is the brand name, same in all languages (per D-07).

**2. Countdown header (lines 106-113).**

Before the `if (state.equals("now"))` block, load the countdown tokens:
```monkey-c
var tokenIn = WatchUi.loadResource(Rez.Strings.CountdownIn) as String;
var tokenNow = WatchUi.loadResource(Rez.Strings.CountdownNow) as String;
```

Update all three `PrayerLogic.formatCountdown()` calls to pass the two new token parameters:
- `PrayerLogic.formatCountdown(0, result["name"] as String, tokenIn, tokenNow);`
- `PrayerLogic.formatCountdown(result["remaining"] as Number, result["name"] as String, tokenIn, tokenNow);`
- (The overnight case uses the same pattern as normal)

**3. `drawEmptyState()` method (lines 234-273).**

Replace the hardcoded `"-- in --"` (line 246) with a loadResource() call:
```monkey-c
var placeholderText = WatchUi.loadResource(Rez.Strings.NoDataPlaceholder) as String;
```
Then use `placeholderText` in the `dc.drawText()` call.

**Summary of hardcoded strings removed from WidgetView:**
- `"Set mosque in"` -> `loadResource(Rez.Strings.WidgetNoMosqueLine1)`
- `"Garmin Connect app"` -> `loadResource(Rez.Strings.WidgetNoMosqueLine2)`
- `"-- in --"` -> `loadResource(Rez.Strings.NoDataPlaceholder)`
- `"in"` and `"now"` tokens -> passed via formatCountdown() parameters from loadResource()
- `"Mawaqit"` -> stays hardcoded (brand name, per D-07)
- `"--:--"` -> stays hardcoded (time format placeholder, not language-dependent)
- Prayer labels from `PrayerLogic.PRAYER_LABELS[i]` -> stays as-is (Arabic names per D-01)
  </action>
  <verify>
    <automated>grep "Rez.Strings.CountdownIn" source/MawaqitWidgetView.mc && grep "Rez.Strings.WidgetNoMosqueLine1" source/MawaqitWidgetView.mc && grep "Rez.Strings.NoDataPlaceholder" source/MawaqitWidgetView.mc && grep -c "loadResource" source/MawaqitWidgetView.mc</automated>
  </verify>
  <acceptance_criteria>
    - source/MawaqitWidgetView.mc contains `WatchUi.loadResource(Rez.Strings.CountdownIn)`
    - source/MawaqitWidgetView.mc contains `WatchUi.loadResource(Rez.Strings.CountdownNow)`
    - source/MawaqitWidgetView.mc contains `WatchUi.loadResource(Rez.Strings.WidgetNoMosqueLine1)`
    - source/MawaqitWidgetView.mc contains `WatchUi.loadResource(Rez.Strings.WidgetNoMosqueLine2)`
    - source/MawaqitWidgetView.mc contains `WatchUi.loadResource(Rez.Strings.NoDataPlaceholder)`
    - source/MawaqitWidgetView.mc does NOT contain hardcoded `"Set mosque in"` (except in comments)
    - source/MawaqitWidgetView.mc does NOT contain hardcoded `"Garmin Connect app"` (except in comments)
    - source/MawaqitWidgetView.mc does NOT contain hardcoded `"-- in --"` (except in comments)
    - source/MawaqitWidgetView.mc still contains hardcoded `"Mawaqit"` (brand name stays)
    - All formatCountdown calls in MawaqitWidgetView.mc pass 4 arguments (remainingSec, name, tokenIn, tokenNow)
  </acceptance_criteria>
  <done>
    - WidgetView uses loadResource() for all localizable strings
    - No hardcoded English display strings remain (except brand name "Mawaqit" and time format "--:--")
    - All formatCountdown() calls updated to pass tokenIn/tokenNow parameters
    - Prayer labels still come from PrayerLogic.PRAYER_LABELS (Arabic names, per D-01)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify localization in simulator</name>
  <files>source/MawaqitGlanceView.mc, source/MawaqitWidgetView.mc, resources/strings/strings.xml, resources-fre/strings/strings.xml</files>
  <action>
Human verification checkpoint. Claude has completed all localization code changes across Plans 01 and 02. The user now verifies visually in the Connect IQ simulator that French and English text displays correctly on all three surfaces (Glance, Widget, Settings).

**What was built:**
Complete French/English localization of all three app surfaces: Glance view, Widget view, and phone app settings. All UI text (countdown tokens, empty states, settings labels) now uses the CIQ resource localization system with loadResource(). French device language shows French text, all other languages fall back to English.

**How to verify:**

Build and run in simulator:
1. Build the project (Cmd+Shift+P > "Monkey C: Build for Device" or via terminal)
2. If build errors occur related to Rez.Strings.*, verify that resources/strings/strings.xml and resources-fre/strings/strings.xml both exist with the correct string IDs

Test English (default):
3. Open Connect IQ Simulator
4. Set simulator language to English (or any non-French language like German -- should show English fallback per LOC-05/D-09)
5. Launch the widget
6. Verify Widget view shows: countdown header like "Asr in 2h 15m" (English "in")
7. If no mosque configured: verify "Mawaqit" title + "Set mosque in" / "Garmin Connect app"
8. If no data: verify "-- in --" placeholder in header
9. Check Glance view shows English countdown text and empty states

Test French:
10. Change simulator language to French (File > Settings or similar)
11. Re-launch the widget
12. Verify Widget countdown header shows "dans" instead of "in" (e.g., "Asr dans 2h 15m")
13. If no mosque configured: verify "Configurer la mosquee" / "dans Garmin Connect"
14. If no data: verify "-- dans --" placeholder
15. Check Glance view shows "dans" in countdown and "Configurer dans l'app Connect" for no-mosque state

Verify prayer names unchanged:
16. In both English and French modes, verify prayer names are: Fajr, Dhuhr, Asr, Maghrib, Isha (Arabic, per D-01)

Verify settings (if possible in simulator):
17. Check phone app settings screen shows French labels when language is French ("ID Mosquee" instead of "Mosque ID")

Resume signal: Type "approved" if all three surfaces display correctly in both languages, or describe any issues found.
  </action>
  <verify>User confirms visual verification passes for both French and English in simulator</verify>
  <done>
    - User has verified French text displays correctly on French device (Glance, Widget, Settings)
    - User has verified English text displays correctly on English/other device (fallback)
    - User has verified prayer names remain Arabic (Fajr, Dhuhr, Asr, Maghrib, Isha) in all languages
    - User typed "approved"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resource files to runtime | Same as Plan 01 -- string resources compiled into binary, no runtime file access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | Widget string resources | accept | Same rationale as T-04-01 -- compiled into PRG binary at build time, no runtime attack surface |
| T-04-05 | Information Disclosure | Widget display strings | accept | All strings are UI labels ("dans", "maintenant", prayer schedule header). Zero sensitive data. |
</threat_model>

<verification>
1. `grep "Rez.Strings.CountdownIn" source/MawaqitWidgetView.mc` returns a match
2. `grep "Rez.Strings.WidgetNoMosqueLine1" source/MawaqitWidgetView.mc` returns a match
3. `grep "Rez.Strings.NoDataPlaceholder" source/MawaqitWidgetView.mc` returns a match
4. No hardcoded English display strings remain in Widget source (except "Mawaqit" brand and "--:--" format)
5. Human verified: French and English display correctly in simulator
</verification>

<success_criteria>
- MawaqitWidgetView.mc uses loadResource() for all localizable strings
- All formatCountdown() calls pass tokenIn/tokenNow parameters
- Simulator shows French text on French device language
- Simulator shows English text on non-French device language (fallback)
- Prayer names remain Arabic in all languages
- Human approval received after visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-language-support/04-02-SUMMARY.md`
</output>
